import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    appBar: {
        marginTop: '40px',
        borderRadius: 15,
        margin: '30px 0',
        display: 'flex',
        fexlDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
    }
}));